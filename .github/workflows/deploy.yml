name: Deploy .NET 8 App to Railway

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore "./src/API on Railway with CICD.csproj"

      - name: Build
        run: dotnet build "./src/API on Railway with CICD.csproj" --configuration Release --no-restore

      - name: Publish
        run: dotnet publish "./src/API on Railway with CICD.csproj" -c Release -o ./publish --self-contained true -r linux-x64 /p:PublishSingleFile=true /p:PublishTrimmed=false

      - name: Install Railway CLI (new)
        run: npm install -g @railway/cli

      - name: Login to Railway
        run: railway login --apiKey ${{ secrets.RAILWAY_API_KEY }}

      - name: Deploy to Railway
        run: railway up --service "API-on-Railway-with-CICD" ./publish
